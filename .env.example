# Base URL (varies per environment: local dev vs production)
APP_BASE_URL="http://localhost:5173"

# Internal authentication (server-only; never expose AUTH_SECRET to client code)
AUTH_ENABLED="true"
AUTH_SECRET="development-only-change-this-secret-1234567890"
